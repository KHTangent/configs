# Add this to the end of .bashrc:
# if [ -f ~/.bash_aliases ]; then
#     . ~/.bash_aliases
# fi


function mkcd() {
	mkdir "$1" && cd "$1"
}

function gcsm() {
	git commit -S -m "$@"
}

export GPG_TTY=$(tty)

eval "$(starship init bash)"

